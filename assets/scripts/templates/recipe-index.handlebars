  <section data-rec-id="{{recipe.id}}" class="recipe">
    <h2 class="recipe-name" data-id="{{recipe.id}}">
      <small>Name:</small>
      {{#if recipe.name}}
        {{recipe.name}}
      {{else}}
        No name provided.
      {{/if}}
    </h2>
    <p>
      <small>Description:</small>
      {{#if recipe.description}}
        {{recipe.description}}
      {{else}}
        No description provided.
      {{/if}}
    </p>

    {{#each recipe.ingredients_list as |ing|}}
    <p class="recipe-ingredient" data-ing-id="{{ing.ingredient_id}}" data-meal-id="{{ing.meal_id}}">{{ing.amount}} {{ing.unit}} of {{ing.ingredient}} &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <span class="ing-edit">Edit</span> <span class="ing-del">Delete</span></p>
    {{/each}}

    {{#each recipe.directions as |dir|}}
    <p class="recipe-direction" data-dir-id="{{dir.id}}">{{dir.step}} &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <span class="dir-edit">Edit</span> <span class="dir-del">Delete</span></p>
    {{/each}}

    <button class="btn btn-add-ing" data-id="{{recipe.id}}">Add Ingredient</button>
    <button class="btn btn-add-dir" data-id="{{recipe.id}}">Add Direction</button>

  </section>
